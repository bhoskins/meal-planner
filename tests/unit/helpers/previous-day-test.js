import {
  previousDay
} from '../../../helpers/previous-day';
import { module, test } from 'qunit';

module('PreviousDayHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = previousDay(42);
  assert.ok(result);
});
